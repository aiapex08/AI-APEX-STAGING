// NAFFCO — QuotationReady page (stub — full implementation in PROMPT 8)
import React from 'react';

export default function QuotationReady() {
  return (
    <div style={{ padding: 40, color: '#e8e6e3', background: '#0c0c0c', minHeight: '100vh' }}>
      <h1 style={{ color: '#34c976' }}>Quotation Ready</h1>
      <p style={{ color: '#b0ada8' }}>PDF download and history table will be built in PROMPT 8.</p>
    </div>
  );
}
